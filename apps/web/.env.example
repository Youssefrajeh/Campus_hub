# Backend API base URL
# Leave empty in local development to use Vite's proxy (/api -> http://localhost:4000)
# In Cloudflare Pages, set this to your hosted backend URL, e.g.:
# VITE_API_URL=https://campushub-api.onrender.com/api
# or https://campushub-api.up.railway.app/api
VITE_API_URL=
