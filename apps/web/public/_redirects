# Proxy API requests directly to the Render backend (Zero CORS, seamless proxying)
/api/*  https://campus-hub-229x.onrender.com/api/:splat  200

# Cloudflare Pages SPA fallback: route all other paths to index.html with 200 status
/*      /index.html                                       200
